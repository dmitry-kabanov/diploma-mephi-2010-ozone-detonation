#ifndef FUNC_IN_TIME_ARRAY_H
#define FUNC_IN_TIME_ARRAY_H

/**
 * Проверяет, находится ли временной шаг j в массиве временных шагов, которые 
 * нужно вывести в файл.
 *
 * @param j временной шаг
 */
bool in_time_array(int j);

#endif